import React from "react";

const InterfaceEditorSidebar = () => {
	return (
		<div className="interface-editor-sidebar-wrapper">
			interface sidebar
		</div>
	);
};

export default InterfaceEditorSidebar;
